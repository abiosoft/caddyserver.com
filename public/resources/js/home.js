$(function() {
/*
	var $spinner = $('#spinner-item-container');

	var slideCount = $('#spinner .item').length;
	var slideIdx = 0;

	setInterval(function() {
		if (slideIdx == slideCount-1)
		{
			// let the last one stay an extra iteration
			slideIdx++;
			return;
		}

		if ((++slideIdx) < slideCount)
		{
			$spinner.animate({
				left: $spinner.position().left - 200
			}, 250);
		}
		else
		{
			$spinner.animate({
				left: 0
			}, 250);
			slideIdx = 0;
		}
	}, 2000);
*/
});